# Copy this file to `.env` and provide your own values.
# Only variables relevant to the production deployment are listed below.

DATABASE_URL="file:./dev.db"
PORT=3000
HOST=0.0.0.0
CLIENT_ORIGIN="http://localhost:8080"

GEMINI_API_KEY="your-gemini-api-key"
GEMINI_MODEL="gemini-1.5-flash"

JWT_SECRET="replace-with-a-strong-secret"

# Optional: configure SMTP to enable email delivery
SMTP_HOST=""
SMTP_PORT=465
SMTP_USER=""
SMTP_PASS=""

LOG_LEVEL=info
NODE_ENV=production
