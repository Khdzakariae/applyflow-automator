# Netlify redirects file
# All routes should serve the index.html for SPA routing
/*    /index.html   200

# API proxy rules (if you have a separate backend)
# /api/*  https://your-backend-url.herokuapp.com/api/:splat  200